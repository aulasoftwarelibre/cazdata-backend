{% extends 'base.html.twig' %}

{% block title %}Hello HomepageController!{% endblock %}

{% block body %}
    <div class="container mx-auto">
        <div class="flex h-screen">
            <div class="m-auto">
                <div class="flex flex-col">
                    <img class="object-none object-center" src="{{ asset('assets/images/logo.svg') }}" alt="CazData Logo">
                    <img class="object-none object-center object-scale-down" src="{{ asset('assets/images/hunter.jpg') }}" alt="Hunter with a dog">
                </div>
            </div>
        </div>
    </div>
{% endblock %}
